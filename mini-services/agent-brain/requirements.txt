# VenueIQ agent brain — the LangGraph decision service.
# Versions pinned to what the local run was verified against.
fastapi==0.115.6
uvicorn==0.30.6
httpx==0.28.1
langgraph==1.2.0
langchain-core==1.4.0
pydantic==2.13.4
python-dotenv==1.0.1
